services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:  
      POSTGRES_USER: auf
      POSTGRES_PASSWORD: auf
      POSTGRES_DB: auf_website
    ports:
      - "5432:5432"
    volumes:
      - auf_pgdata:/var/lib/postgresql/data

volumes:
  auf_pgdata:
